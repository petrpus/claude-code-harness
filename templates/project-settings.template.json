{
  "_comment": "Baseline permissions + deny for code-dev projects using claude-code-harness. Copy to .claude/settings.json in the project, then add project-specific WebFetch domains / Read paths / extra allows on top.",
  "_policy_layering": "Three layers, single source of truth each: (L1) this deny list = static string patterns (force-push, rm -rf /, reading .env). (L2) hooks/pre-bash.sh = context-dependent logic that a static pattern can't express (push FROM main needs the branch). (L3) skills like commit-agent = workflow advice that references, never re-implements, L1/L2. Don't duplicate a rule across layers.",
  "_deny_globs": "A deny glob matches a SUBSTRING of the command, so a short flag must be anchored on whitespace or it matches inside operands. `Bash(git push *-f*)` denied every branch whose name contained '-f' — feat/...-full, fix/...-first, feat/...-filter — and surfaced as a bare permission error that reads like the user declined. Short-flag denies are therefore written as three patterns: `<cmd> -f*` (flag first, covers bundles like -fu), `<cmd> * -f *` (flag in the middle), `<cmd> * -f` (flag last). Long forms keep a plain `*--force*`, which is prefix-shaped and cannot match inside a ref name. The breadth of `rm -rf /*` is DELIBERATE and not the same bug: over-denying a destructive command costs one permission prompt, under-denying costs the machine, and hooks/pre-bash.sh already resolves root-ish targets with context. Don't 'fix' it.",
  "_verify_gate": "Verification is reminders by default (the harness Stop hooks always exit 0). For UNATTENDED runs you can opt into a hard gate: register templates/require-verify-before-stop.sh as a Stop hook here (hooks.Stop[].hooks[].command) — it exits 2 until tmp/.last-verify-status is a fresh 'ok', blocking the turn from ending. It is opt-in only and never wired into the plugin's hooks/hooks.json (ADR-0002); Claude Code lifts the block after 8 refusals so it can't deadlock. autopilot enables it for the duration of a run and removes it on run end.",
  "permissions": {
    "allow": [
      "Edit(**/*.md)",
      "Write(**/*.md)",
      "Bash(rm **/*.md)",

      "Bash(pnpm:*)",
      "Bash(npm run:*)",
      "Bash(npx:*)",
      "Bash(node:*)",
      "Bash(tsx:*)",

      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git branch*)",
      "Bash(git checkout *)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git fetch*)",
      "Bash(git pull*)",
      "Bash(git push *)",
      "Bash(git rebase *)",
      "Bash(git stash*)",

      "Bash(gh pr *)",
      "Bash(gh issue *)",
      "Bash(gh api *)",
      "Bash(gh repo *)",

      "Bash(docker:*)",
      "Bash(docker compose:*)",
      "Bash(prisma:*)",
      "Bash(playwright:*)",
      "Bash(vitest:*)",

      "Edit(app/**)",
      "Write(app/**)",
      "Edit(tests/**)",
      "Write(tests/**)",
      "Edit(scripts/**)",
      "Write(scripts/**)",
      "Edit(prisma/**)",
      "Write(prisma/**)",
      "Edit(public/**)",
      "Write(public/**)",
      "Edit(*.config.{ts,js,json})",
      "Write(*.config.{ts,js,json})",
      "Edit(package.json)",
      "Write(package.json)",
      "Edit(.gitignore)",
      "Write(.gitignore)",
      "Edit(.env.example)",
      "Write(.env.example)",
      "Edit(.claude/**)",
      "Write(.claude/**)",
      "Edit(.github/**)",
      "Write(.github/**)",
      "Edit(docs/**)",
      "Write(docs/**)",
      "Edit(tmp/**)",
      "Write(tmp/**)",

      "WebSearch",
      "WebFetch(domain:github.com)",
      "WebFetch(domain:raw.githubusercontent.com)",
      "WebFetch(domain:reactrouter.com)",
      "WebFetch(domain:www.prisma.io)",
      "WebFetch(domain:tailwindcss.com)",
      "WebFetch(domain:ui.shadcn.com)"
    ],
    "deny": [
      "Bash(git push *--force*)",
      "Bash(git push -f*)",
      "Bash(git push * -f *)",
      "Bash(git push * -f)",
      "Bash(git reset --hard*)",
      "Bash(git clean *--force*)",
      "Bash(git clean -f*)",
      "Bash(git clean * -f *)",
      "Bash(git clean * -f)",
      "Bash(rm -rf /*)",
      "Bash(rm -rf .git*)",
      "Write(.env)",
      "Edit(.env)",
      "Read(.env)",
      "Read(.env.*)"
    ],
    "additionalDirectories": ["/tmp"]
  }
}
